pre-commit:
  jobs:
    - name: biome
      glob: "*.{js,jsx,ts,tsx,css,json}"
      run: bunx biome check --fix {staged_files}
      stage_fixed: true

commit-msg:
  jobs:
    - name: commitlint
      run: bunx commitlint --edit {1}
